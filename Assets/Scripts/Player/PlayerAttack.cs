using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAttack : MeleeWeapon
{
    public PlayerAttack(string name, float distance, float speed, float damage) : base(name, distance, speed, damage)
    {
    }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
